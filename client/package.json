{
  "name": "gemstone-smalltalk-client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "selfsigned": "^5.5.0",
    "vscode-languageclient": "^10.1.1"
  },
  "devDependencies": {
    "@types/jsdom": "^30.0.0",
    "@types/vscode": "~1.101.0",
    "@vitest/coverage-v8": "^5.0.0",
    "css.escape": "^1.5.1",
    "dotenv": "^17.4.2",
    "jsdom": "^30.0.1",
    "vitest": "^5.0.0",
    "vscode-uri": "^3.2.0"
  },
  "scripts": {
    "//test": "Runs only the 'default' vitest project (see vitest.config.ts). The 'gci' project is on-demand (needs a live stone) and is excluded here on purpose; run it with `npm run test:gci`. Without --project, vitest would run both projects.",
    "test": "vitest run --project default",
    "//test:coverage": "Line/branch coverage for the default project, v8 provider. Takes a path prefix to scope the report, e.g. `npm run test:coverage --workspace client -- src/undo`; with no argument it reports on everything the suite loaded. The whole suite runs either way — a file is only reported as uncovered if no test anywhere exercised it.",
    "test:coverage": "vitest run --project default --coverage.enabled --coverage.provider=v8 --coverage.reporter=text --coverage.exclude='**/__tests__/**' --coverage.exclude='**/__mocks__/**' --coverage.include",
    "test:server:start": "./bin/gs-test-server.sh --start",
    "//test:server:start:rowan3": "Starts a 3.7.5 stone on the rowan3 extent — the configuration the Tonel file out/in suites require (see src/queries/tonel/tonelCapability.ts). Those suites SKIP on any other stone, so a green `npm test` against the default stone says nothing about that feature. Repoints .env.test at this stone.",
    "test:server:start:rowan3": "./bin/gs-test-server.sh --start 3.7.5 jasper-rowan3 --extent extent0.rowan3.dbf",
    "test:server:stop:rowan3": "./bin/gs-test-server.sh --stop 3.7.5 jasper-rowan3",
    "test:server:stop": "./bin/gs-test-server.sh --stop",
    "test:server:list": "./bin/gs-test-server.sh --list"
  }
}
